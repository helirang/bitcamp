#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char** argv)
{
	char ch;
	int digit = 0; //숫자 갯수
	int alpha = 0; //영문자
	int words = 0; // 띄어쓰기
	int numbers = 0; //정수들의 총합
	while ((ch = getchar()) != EOF)
	{

	}
	printf("\n");
}
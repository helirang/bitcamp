#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// 도구 - 옵션 - 환경 - 글꼴 -Consolas
int main(int argc, char** argv)
{
	//ctrl k +c = 주석, k+u 는 주석 해제
	int num = 100;
	char ch = 'A';
	double fnum = 3.14;

	char str[] = "Korean";

	printf("정수 %d ", num);
	// 정수 %d, 문자 %c, 실수 %lf, 문자열 %s
	// d는 영어로 십진수



	return 0;
}
